server.port=8081
# E2E tests run against the production bundle — this is what actual users
# will ship. Avoids dev-bundle compile flakiness on first page load.
vaadin.productionMode=true
logging.level.org.atmosphere=warn
